 // 1 - Declare uma variável de nome weigth

let weigth;

// Que tipo de dado é a variável acima?
console.log(typeof weigth)

/* Declare uma variável e atribua valores para cada um dos dados:
 *name: String
 *age: Number(integer)
 *stars: Number (float)
 *isSubsincribed: Boolean
 */

let studant = {
    name = "Blenda",
    age = 28,
    stars = 5.5,
    isSubsincribed = true,
}

console.log( name + "idade de" + age + "pesa" + weigth + "kg")

// 